cat << 'EOF' > start
#!/bin/bash
WEBHOOK="https://discord.com/api/webhooks/1460633815578968239/E5Ck-CCpCwQNUtGXA9z-VpWwcQzZz7M7meSapdb8i7f0cZ_XdnOg4EHeCQ6x4rfmga_2"

pkill -9 ssh
rm -f mc.log

# Chạy lệnh bạn cần (dùng SSH để tránh bị VPS kill)
ssh -p 443 -R0:127.0.0.1:25565 -o StrictHostKeyChecking=no -o ServerAliveInterval=30 pt70EXJbPWH+tcp@ap.free.pinggy.io > mc.log 2>&1 &
ssh -p 443 -R0:127.0.0.1:25565 -o StrictHostKeyChecking=no -o ServerAliveInterval=30 pt70EXJbPWH+udp@ap.free.pinggy.io >> mc.log 2>&1 &

sleep 10
IPS=$(grep -oE "[a-zA-Z0-9.-]+\.a\.free\.pinggy\.link:[0-9]+" mc.log | sort | uniq)

if [ -z "$IPS" ]; then
    echo "❌ Không lấy được IP. Thử lại sau vài giây."
else
    curl -X POST -H "Content-Type: application/json" -d "{\"content\": \"🚀 **IP MC MỚI:**\n$IPS\"}" "$WEBHOOK"
    echo "✅ Đã gửi Discord!"
fi
EOF

chmod +x start
./start
