cat << 'EOF' > start
#!/bin/bash
WEBHOOK="https://discord.com/api/webhooks/1460633815578968239/E5Ck-CCpCwQNUtGXA9z-VpWwcQzZz7M7meSapdb8i7f0cZ_XdnOg4EHeCQ6x4rfmga_2"

# Dọn dẹp các tiến trình cũ
pkill -9 ssh
pkill -9 pinggy

# Chạy 2 lệnh chính (Bỏ lưu log phức tạp để tránh bị IDX soi)
ssh -p 443 -R0:127.0.0.1:25565 -o StrictHostKeyChecking=no -o ServerAliveInterval=30 pt70EXJbPWH+tcp@ap.free.pinggy.io > /dev/null 2>&1 &
ssh -p 443 -R0:127.0.0.1:25565 -o StrictHostKeyChecking=no -o ServerAliveInterval=30 pt70EXJbPWH+udp@ap.free.pinggy.io > /dev/null 2>&1 &

# Gửi thông báo nhanh lên Discord
curl -X POST -H "Content-Type: application/json" -d "{\"content\": \"🚀 **Server Minecraft đã được kích hoạt!**\n(Vào trang chủ Pinggy để lấy IP hoặc xem trực tiếp trên Terminal)\"}" "$WEBHOOK"

echo "✅ Đã kích hoạt 2 lệnh chạy ngầm."
echo "👉 Bạn có thể gõ 'ps aux | grep ssh' để kiểm tra xem nó còn sống không."
EOF

chmod +x start
./start
